<%= form_with(model: share, local: true) do |f| %>
<%#= form_for([@post, @post.shares.build])do |f| %>
  <% if share.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(share.errors.count, "error") %> prohibited this share from being saved:</h2>

      <ul>
      <% share.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
   <%= f.hidden_field :post_id, :class => "form-control post_id_val"%>
  </div>

   <div class="form-group">
    <%= f.hidden_field :shared_id, :class => "form-control post_id_val", :value => current_user.id%>
  </div>

  <div class="form-group">
    <%= f.label :user_id %>
   <%#= f.select :user_id, User.pluck(:username), {}, {class: 'form-control'}%>
    <%#= f.collection_select(:user_id, User.all, :id, :username, {prompt: false}, {:class=>'form-control', :multiple => true}) %>
    <%= f.select :user_id, User.all.collect { |x| [x.username, x.id]}, {:include_hidden => false}, {:class=>'form-control',:multiple => true} %>
  </div>
    <%= f.submit :class => "btn btn-primary pull-right", value: 'share' %>
<% end %>
<%= link_to 'Back', posts_path %>



